* text=auto eol=lf

*.png binary
*.jpg binary
*.parquet binary
*.ipynb text eol=lf

# No nbstripout clean filter. It strips outputs on `git add`, before any hook
# runs, so removing the pre-commit hook alone silently achieves nothing. The
# notebooks are committed with their outputs. See DECISION.md.
*.ipynb diff=ipynb
